{
  "private": true,
  "name": "discord-wholesome-bot",
  "version": "1.0.0",
  "description": "Discord bot for sending wholesome photos",
  "main": "./src/index.js",
  "scripts": {
    "start": "wrangler dev",
    "publish": "wrangler publish",
    "format": "prettier --write './src/**/*.{js,css,json,md}'"
  },
  "license": "MIT",
  "dependencies": {
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^1.15.0",
    "husky": "^6.0.0",
    "lint-staged": "^10.5.4",
    "prettier": "^1.18.2"
  },
  "lint-staged": {
    "./src/**/*.{js}": "npm run format"
  }
}
